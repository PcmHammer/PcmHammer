<Window x:Class="PCMHammer.Views.ChangeVINWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PCMHammer.Views"
        mc:Ignorable="d"
        Title="Change VIN" Height="181" Width="485"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Enter New VIN:" FontWeight="Bold" Margin="0,0,0,5"/>

        <TextBox Grid.Row="1" 
                 Text="{Binding Vin, UpdateSourceTrigger=PropertyChanged}" 
                 d:Text="VIN GOES HERE"
                 FontFamily="Consolas" FontSize="16" CharacterCasing="Upper" MaxLength="17" Padding="5"/>

        <TextBlock Grid.Row="2" 
                   Text="{Binding ValidationPrompt}" 
                   d:Text="Validation message goes here"
                   TextWrapping="Wrap" Margin="0,10,0,0"/>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Grid.ColumnSpan="2">
            <Button Content="OK" Command="{Binding OkCommand}" Width="80" Height="26" Margin="0,0,10,0" IsDefault="True"/>
            <Button Content="Cancel" Command="{Binding CancelCommand}" Width="80" Height="26" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
