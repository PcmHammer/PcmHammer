<Window x:Class="PCMHammer.Views.DialogBoxes.WriteOperationDialogBox"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        SizeToContent="WidthAndHeight"
        Title="Write Type">

    <Grid Margin="12">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Label Content="PCM Type" VerticalAlignment="Center" Target="{Binding ElementName=PCMTypeComboBox}"/>
        <ComboBox x:Name="PCMTypeComboBox" Grid.Column="1" Margin="10,5" 
                  IsEditable="False" ItemsSource="{Binding PCMTypes}" 
                   SelectedItem="{Binding SelectedPCMType, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
            <ComboBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock x:Name="PcmTypeTextBlock" Text="{Binding}"/>
                    <DataTemplate.Triggers>
                        <DataTrigger Binding="{Binding}" Value="Undefined">
                            <Setter TargetName="PcmTypeTextBlock" Property="Text" Value="Auto (Query OSID)"/>
                        </DataTrigger>
                    </DataTemplate.Triggers>
                </DataTemplate>
            </ComboBox.ItemTemplate>
        </ComboBox>

        <Label Content="Write Type" VerticalAlignment="Center" Grid.Row="1" Target="{Binding ElementName=WriteTypeComboBox}"/>
        <ComboBox x:Name="WriteTypeComboBox" Grid.Column="1" Grid.Row="1" Margin="10,5"
                  IsEditable="False" ItemsSource="{Binding WriteTypes}" 
                  SelectedItem="{Binding SelectedWriteType, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
            <ComboBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock x:Name="DisplayTextBlock" Text="{Binding}"/>
                    <DataTemplate.Triggers>
                        <DataTrigger Binding="{Binding}" Value="Full">
                            <Setter TargetName="DisplayTextBlock" Property="Text" Value="Clone (Full Flash)"/>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding}" Value="OsPlusCalibrationPlusBoot">
                            <Setter TargetName="DisplayTextBlock" Property="Text" Value="OS, Calibration, Boot"/>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding}" Value="Parameters">
                            <Setter TargetName="DisplayTextBlock" Property="Text" Value="Parameters"/>
                        </DataTrigger>
                    </DataTemplate.Triggers>
                </DataTemplate>
            </ComboBox.ItemTemplate>
        </ComboBox>

        <!-- TODO: This will be used to silence the brick warning when using the "reset pin" to bypass the standard initialization sequence -->
        <CheckBox Content="PCM is in Recovery Mode" Grid.Row="2" Grid.ColumnSpan="2" Margin="0,5" HorizontalAlignment="Center"
                  IsChecked="{Binding IsRecoveryMode, Mode=TwoWay}" Visibility="Collapsed"/>

        <Grid Grid.Column="1" Grid.Row="3" Margin="10,5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Button x:Name="CancelButton" Content="Cancel" Margin="0,0,8,0" HorizontalAlignment="Stretch"
                    IsCancel="True" Command="{Binding CloseCommand}"/>
            <Button x:Name="OkButton" Content="OK" Grid.Column="1" HorizontalAlignment="Stretch"
                    IsDefault="True" Command="{Binding AcceptAndCloseCommand}"/>
        </Grid>
    </Grid>
</Window>