<Window x:Class="PCMHammer.Views.DialogBoxes.PcmTypeSelectDialogBox"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PCMHammer.Views.DialogBoxes"
        mc:Ignorable="d"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        SizeToContent="WidthAndHeight"
        Title="PCM Type">
    <Grid Margin="12">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Label Content="PCM Type:" VerticalAlignment="Center" Target="{Binding ElementName=PCMTypeComboBox}"/>
        <ComboBox x:Name="PCMTypeComboBox" Grid.Column="1" Margin="10,5" 
                  IsEditable="False" ItemsSource="{Binding PCMTypes}" 
                  SelectedItem="{Binding SelectedPCMType, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
            <ComboBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock x:Name="PcmTypeTextBlock" Text="{Binding}"/>
                    <DataTemplate.Triggers>
                        <DataTrigger Binding="{Binding}" Value="Undefined">
                            <Setter TargetName="PcmTypeTextBlock" Property="Text" Value="Auto (Query OSID)"/>
                        </DataTrigger>
                    </DataTemplate.Triggers>
                </DataTemplate>
            </ComboBox.ItemTemplate>
        </ComboBox>

        <Grid Grid.Column="1" Grid.Row="2" Margin="10,5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Button x:Name="CancelButton" Content="Cancel" Margin="0,0,8,0" HorizontalAlignment="Stretch"
                    IsCancel="True" Command="{Binding CloseCommand}"/>
            <Button x:Name="OkButton" Content="OK" Grid.Column="1" HorizontalAlignment="Stretch"
                    IsDefault="True" Command="{Binding AcceptAndCloseCommand}"/>
        </Grid>
    </Grid>
</Window>
