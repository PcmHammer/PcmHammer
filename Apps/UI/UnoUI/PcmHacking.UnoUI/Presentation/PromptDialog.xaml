<ContentDialog x:Class="PcmHacking.UnoUI.Presentation.PromptDialog"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:local="using:PcmHacking.UnoUI.Presentation"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:uen="using:Uno.Extensions.Navigation.UI"
      xmlns:utu="using:Uno.Toolkit.UI"
               
      Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
            Text="{Binding BodyText}"
            HorizontalAlignment="Center"
            VerticalAlignment="Top"
            Margin="0,20"
            Padding="10,10"/>

        <ComboBox Grid.Row="1"
                  Name="ItemComboBox"
                  Visibility="{Binding ShowComboBox}"
                  IsEnabled="{Binding ShowComboBox}"
                  ItemsSource="{Binding ComboBoxItems}"
                  SelectedItem="{Binding ComboBoxSelection}"/>

    </Grid>
</ContentDialog>
