<Page
    x:Class="PcmHacking.UnoUI.Presentation.ReadPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:PcmHacking.UnoUI.Presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>
    
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="Auto" />
      <ColumnDefinition Width="Auto" />
    </Grid.ColumnDefinitions>

      <Button Grid.Row="0" Grid.Column="0" Content="Start Reading" Margin="10"
              Command="{Binding Start}"
              IsEnabled="{Binding StartEnabled}"/>
      <Button Grid.Row="0" Grid.Column="1" Content="Cancel" Margin="10"
              Command="{Binding Cancel}"
              IsEnabled="{Binding CancelEnabled}"/>

    <ProgressBar Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" Margin="10"/>

    <ScrollViewer Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2">
      <TextBlock Text="Waiting to begin..." Margin="10" FontFamily="Courier New"/>
    </ScrollViewer>
  </Grid>

</Page>
