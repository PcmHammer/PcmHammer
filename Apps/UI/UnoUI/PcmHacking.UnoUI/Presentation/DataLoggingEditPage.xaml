<ContentDialog x:Class="PcmHacking.UnoUI.Presentation.DataLoggingEditPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:local="using:PcmHacking.UnoUI.Presentation"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:uen="using:Uno.Extensions.Navigation.UI"
      xmlns:utu="using:Uno.Toolkit.UI"
      Loaded="ContentDialog_Loaded"
      Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" x:Name="Labels" />
            <RowDefinition Height="4*" x:Name="UpperHalf" />
            <RowDefinition Height="4*" x:Name="LowerHalf" />
            <RowDefinition Height="*" x:Name="Whitespace" />
            <RowDefinition Height="*" x:Name="Buttons" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0"
                   Grid.Column="0"
                   Grid.ColumnSpan="2"
                   Text="Parameter"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"
                   Margin="5" />

        <TextBlock Grid.Row="0"
                   Grid.Column="1"
                   Text="Units"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"
                   Margin="5" />

        <Border   Grid.Row="1"
                  Grid.RowSpan="2">
            <ListView 
                  Grid.Column="0"   
                  Grid.ColumnSpan="2"
                  Name="Parameters"
                  ItemsSource="{Binding ParameterList}"
                  BorderThickness="1"
                  Margin="5" />
        </Border>

        <Border   Grid.Row="1"
                  Grid.Column="1">
            <ListView
                  Name="Conversions"
                  ItemsSource="{Binding ConversionList}"
                  BorderThickness="1"
                  Margin="5" />
        </Border>

        <CheckBox 
                  Grid.Row="2"
                  Grid.Column="1"
                  Content="Zoom"
                  IsChecked="{Binding Zoom}"
                  Margin="5" />

        <Button Grid.Row="4"
                Grid.Column="0"
                Width="80"
                HorizontalAlignment="Right"
                VerticalAlignment="Center"
                Margin="5" 
                Content="Delete Parameter" 
                Command="{Binding DeleteClicked}"/>

        <Button Grid.Row="4"
                Grid.Column="1"
                Width="80"
                HorizontalAlignment="Right"
                VerticalAlignment="Center"
                Margin="5" 
                Content="Apply Changes" 
                Command="{Binding ApplyClicked}"/>

        <Button Grid.Row="4"
                Grid.Column="2"
                Width="80"
                HorizontalAlignment="Left"
                VerticalAlignment="Center"
                Margin="5" 
                Content="Cancel" 
                Command="{Binding CancelClicked}"/>
    </Grid>
</ContentDialog>
